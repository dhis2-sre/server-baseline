# Nothing in here is contacted: the checks in run.sh stop at --syntax-check. The host exists because
# a play needs something to compile against, and ansible_user because both bootstrap and harden
# default docker_user from it.
[servers]
syntax-check-only ansible_host=127.0.0.1 ansible_user=ubuntu
