# server-baseline

Ansible roles that turn a stock Ubuntu 24.04 host into a hardened Docker host, shared by the DHIS2
projects that run workloads on plain servers so the hardening has one implementation rather than one
per repository.
