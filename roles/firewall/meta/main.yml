galaxy_info:
  author: DHIS2 SRE
  description: Default deny DOCKER-USER chain, persisted across reboots.
  license: Apache-2.0
  min_ansible_version: '2.15'
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - docker
    - firewall
    - iptables
