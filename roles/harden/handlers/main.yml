- name: Restart docker
  ansible.builtin.service:
    name: docker
    state: restarted

# Reload, not restart: sshd re-reads its configuration without dropping the connection this play is
# running over. New connections get the hardened settings immediately.
- name: Reload sshd
  ansible.builtin.service:
    name: ssh
    state: reloaded
